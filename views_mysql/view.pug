html
    head
        meta(charset='utf-8')
    body
        h1 
            a(href='/topic') Server Side Javascript
        ul
         each val in topics
            li
                a(href='/topic/'+val.id)= val.title
        article
            if topic
                h2= topic.title
                p= topic.description
                div= 'by ' + topic.author
            else
                h2 Welcome
                | This is serve side javascript tutorial.
        ul
            li
                a(href='/topic/add') add 
            if topic 
                li
                    a(href='/topic/' + topic.id + '/edit') edit
                li
                    a(href='/topic/' + topic.id + '/del') delete
                
        